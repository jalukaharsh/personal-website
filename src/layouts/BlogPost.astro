---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  tags?: string[];
}

const { title, description, pubDate, tags } = Astro.props;

const formattedDate = pubDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
});
---

<BaseLayout title={title} description={description}>
  <Header slot="header" />
  
  <article class="prose prose-lg max-w-none">
    <div class="mb-8">
      <a 
        href="/blog"
        class="text-primary-600 hover:text-primary-800 text-sm font-semibold no-underline"
      >
        ← Back to Blog
      </a>
    </div>

    <header class="mb-8">
      <h1 class="text-4xl font-bold mb-4">{title}</h1>
      
      <div class="flex items-center gap-4 text-secondary-600 mb-4">
        <time datetime={pubDate.toISOString()} class="text-sm">
          {formattedDate}
        </time>
      </div>

      {tags && tags.length > 0 && (
        <div class="flex flex-wrap gap-2">
          {tags.map(tag => (
            <a 
              href={`/blog/tag/${tag}`}
              class="text-xs bg-primary-100 text-primary-800 px-3 py-1 rounded-full hover:bg-primary-200 transition-colors no-underline"
            >
              {tag}
            </a>
          ))}
        </div>
      )}
    </header>

    <div class="prose-content">
      <slot />
    </div>
  </article>

  <Footer slot="footer" />
</BaseLayout>
