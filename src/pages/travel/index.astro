---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ContentListItem from '../../components/ContentListItem.astro';
import WorldMapWithCities from '../../components/WorldMapWithCities.astro';

const allPosts = await getCollection('travel', ({ data }) => {
  return data.draft === false;
});

const sortedPosts = allPosts.sort((a, b) => {
  return b.data.pubDate.valueOf() - a.data.pubDate.valueOf();
});
---

<BaseLayout 
  title="Travel - Harsh Jaluka"
>
  <Header slot="header" />

  <div class="main-container">
    <section class="section">
      <p class="page-intro">
        I've been lucky to have had lots of opportunity to travel in my life, and I've plotted out the cities I've been to in map below. I prefer tracking cities over countries because countries is just an objectively bad metric. For example, travelling through Europe will give you a boost by 30 countries while travelling through India just one despite India arguably being just as culturally diverse a region. 
      </p>

      <p class="page-intro"> 
        I've also written articles, listed below, where I share stories, insights, and practical tips from my travels. One day, clicking on the city dots will take you to the articles directly, but not today. 
      </p>
    </section>
  </div>
  
  <div class="world-map-container-full" style="margin-bottom: 3rem;">
    <WorldMapWithCities />
  </div>

  <div class="main-container">
      
    <div class="mb-8">
      <div class="flex justify-between items-center mb-8">
        <p class="travel-count">
          {sortedPosts.length} {sortedPosts.length === 1 ? 'post' : 'posts'}
        </p>

        <div class="flex gap-2 flex-wrap items-center">
        <span class="sort-label">Sort by:</span>
        <button 
          id="sort-title" 
          class="sort-btn sort-btn-active"
        >
          Name
        </button>
        <span class="sort-separator">|</span>
        <button 
          id="sort-author" 
          class="sort-btn"
        >
          Length of Stay
        </button>
        <span class="sort-separator">|</span>
        <button 
          id="sort-year" 
          class="sort-btn"
        >
          Last Updated
        </button>
        </div>
      </div>
    </div>

    <ul class="space-y-4">
      {sortedPosts.map(post => (
        <ContentListItem
          title={post.data.title}
          slug={post.slug}
          collection="travel"
          metadata={post.data.pubDate.toLocaleDateString('en-US', { 
            year: 'numeric', 
            month: 'long', 
            day: 'numeric' 
          })}
        />
      ))}
    </ul>

    {sortedPosts.length === 0 && (
      <div class="text-center py-12">
        <p class="empty-message">No travel posts yet. Check back soon!</p>
      </div>
    )}
  </div>

  <Footer slot="footer" />
</BaseLayout>

<style>

  .travel-count {
    color: var(--color-text-on-dark);
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-6);
  }

  .empty-message {
    color: var(--color-text-muted);
    font-size: var(--font-size-lg);
    margin-top: var(--spacing-24);
    margin-bottom: var(--spacing-24);
  }

  .world-map-container-full {
    width: 100%;
    margin: var(--spacing-8) auto 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .sort-label {
    color: var(--color-text-on-dark);
  }

  .sort-separator {
    color: var(--color-text-muted);
  }

  .sort-btn {
    color: var(--color-text-on-dark);
    border-bottom: 2px solid transparent;
    padding-bottom: 4px;
    background: transparent;
    cursor: pointer;
    transition: var(--transition-colors);
    transition-delay: var(--transition-delay-none);
  }

  .sort-btn:hover {
    color: var(--color-interactive); 
    background: transparent;
  }

  .sort-btn-active {
    border-bottom-color: var(--color-text-on-dark);
    font-weight: var(--font-weight-medium);
  }

</style>
