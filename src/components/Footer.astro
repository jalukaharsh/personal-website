---
const currentYear = new Date().getFullYear();
---

<footer class="footer-elegant mt-auto w-full flex justify-center">
  <div class="w-full max-w-6xl px-4" style="padding-top: 3rem; padding-bottom: 3rem;">
    <div class="text-center footer-text">
      <p>&copy; {currentYear} Harsh Jaluka. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .footer-elegant {
    background-color: var(--color-bg-primary);
    border-top: 1px solid var(--color-border-subtle);
  }

  .footer-text {
    color: var(--color-text-on-dark);
  }

  footer a {
    color: var(--color-interactive-secondary);
    text-decoration: none;
    transition: var(--transition-colors);
    transition-delay: var(--transition-delay-none);
  }

  footer a:hover {
    color: var(--color-electric-blue);
    text-decoration: underline;
  }

  footer a:focus-visible {
    outline: 2px solid var(--color-interactive-secondary);
    outline-offset: 2px;
    border-radius: var(--radius-sm);
    transition-delay: var(--transition-delay-none);
  }
</style>
